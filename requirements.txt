# HealthLink dependencies — unpinned to install the latest compatible releases.
# Floors only where a minimum is functionally required (package renames, API breaks).
# Tested on Python 3.11+.

# FastAPI and Server
fastapi>=0.115.0            # TestClient needs httpx>=0.28 support
uvicorn[standard]>=0.30.0
pydantic>=2.7.4            # required by langchain 1.x
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# LangChain 1.x with Google GenAI integration (Python 3.10+)
langchain>=1.0.0
langchain-core>=1.0.0
langchain-text-splitters>=1.0.0
langchain-google-genai>=3.0.0

# Vector Store (package renamed from pinecone-client to pinecone)
pinecone>=5.0.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# Utilities
python-dotenv>=1.0.1
httpx>=0.28.0
tenacity>=8.5.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.0

# Streamlit UI
streamlit>=1.37.0
plotly>=5.22.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0